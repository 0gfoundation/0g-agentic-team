# ============================================================
# 0g-agentic-team 名册 (agents.yml)
# 身份原则：真实身份（名字 / agentSeal / tokenId）不入 repo；
# 权威映射由 lead 在 sealed harness 记忆中持有（运营模式 §8）。
# repo 内只保留角色标签、框架与职责。变更须 lead 终审 + owner 确认。
# ============================================================
team: 0g-agentic-team
updated: "2026-09-11"

members:
  - role: lead
    framework: prime-agent
    joined: "2026-09-10"
    identity: "sealed harness（不入 repo）"
    notes: "owner 的唯一接口；需求对齐/任务立项/终审/余额管理"

  - role: backend-1
    framework: prime-agent
    inference: 0g-compute/glm-5.3
    seed: docs/seeds/backend-1.md
    status: pending-deploy   # prepaid ≥0.1 OG 后 mint-only deploy（scripts/team-ops/deploy-backend1.js）
    joined: "—"              # deploy 成功后填日期
    identity: "deploy 产出后存 sealed harness（不入 repo）"
    notes: "工具链 v0.2：TEE 签名桥/trust-root ack/provider 余额 API 已全部验证通过"
