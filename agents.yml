# ============================================================
# 0g-agentic-team roster (agents.yml)
# Identity principle: real identities (name / agentSeal / tokenId) never enter the repo;
# the authoritative mapping is held by the lead in sealed harness memory (operating model §8).
# Only role labels, framework, and responsibilities live in the repo.
# Changes require lead final review + owner confirmation.
# ============================================================
team: 0g-agentic-team
updated: "2026-09-11"

members:
  - role: lead
    framework: prime-agent
    joined: "2026-09-10"
    identity: "sealed harness (not in repo)"
    notes: "the owner's single interface; requirement alignment / task initiation / final review / balance management"

  - role: backend-1
    framework: prime-agent
    inference: 0g-compute/glm-5.3
    seed: docs/seeds/backend-1.md
    status: minted       # on-chain identity minted (offline; no runtime cost until start())
    joined: "2026-09-11"
    identity: "sealed harness (not in repo)"
    notes: "toolchain v0.2; TEE bridge / ack / provider API verified; mint tx recorded in PR"
